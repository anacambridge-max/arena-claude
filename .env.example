# Required: Upstox Developer Apps -> Analytics -> Generate Token
# Keep this server-side. Never expose or commit the real value.
UPSTOX_ANALYTICS_TOKEN=

# Optional: only needed by other database-backed features you add later.
DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:5432/app_db
